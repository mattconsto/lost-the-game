package Map;

import TileSystem.Tile;
import TileSystem.TileSystem;

/**
 * Created by Andy on 25/01/2015.
 */
public class MapFixer {
    public static void fixMap(Tile[][] tiles) {
        for (int x = 0; x < tiles.length; x++) {
            for (int y = 0; y < tiles[x].length; y++) {
                Tile tile = tiles[x][y];

            }
        }
    }
}
